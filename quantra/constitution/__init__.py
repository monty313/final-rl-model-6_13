"""quantra.constitution  —  SOW tier: 00_constitution.

WHAT THIS PACKAGE DOES
----------------------
Holds the non-negotiables as importable, machine-checkable constants and the
mission/rules/safety documents: the constitutional rules (C1-C9), the single
mission, and the safety boundaries that bound the LLM Risk Doctor and live
execution.

HOW IT SERVES REPEATED FTMO-STYLE PASSING
-----------------------------------------
These are the commitments that keep every other module pointed at *passing*, not
profit: one universal policy, laws-as-masks, Layer-0 dominance, real costs day 1,
read-only diagnostics. Other packages import these so the invariants are enforced
in code, not just prose.

BINDING RULEBOOK FOR THE LLM RISK DOCTOR: ``docs/MLP_INTERPRETABILITY_LAYER.md``.
"""
